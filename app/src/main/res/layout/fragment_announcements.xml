<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.arbrr.onehack.ui.announcements.AnnouncementsFragment"
    android:id="@+id/announcements_fragment_container">

    <!-- Toolbar shadow because fuck you elevation and tab strips -->
    <include layout="@layout/toolbar_shadow"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/announcement_rv"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>
